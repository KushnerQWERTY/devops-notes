Base commands: 

Block 1 Common

whoami - Показывает текущего пользователя
hostname - имя машины pwd - показывает путь, по которому я сейчас нахожусь
ls - показывает наличие текущей папки; ls -l - показ файлов в текущей директории построчно; ls "папка" - посмотреть содержимое определенной папки
mkdir "имя" - создание папки
cd - переход в другие директории
touch "имя.формат" - создание файлов
nano - текстовой редактор
cat "Имя файла" - прочитать файл
sudo - запуск от имени администратора
uname -a - показывает версию ядра
apt update - обновления; apt update -y - обновление с согласием на все.
xargs "command" - выполняет команду n-ое раз

Block 2 - Actions with files

cp "Имя копируемого файла" "имя скопированного файла" - копирования файла
mv "имя файла, которое нужно переименовать" "желаемое имя файла" - переименование
mv "имя файла для перемещения" "папка, куда нужно перенести" 
rm "имя файла или путь" - удаление файла или папки; rmdir "имя папки или путь" - удаление папки (Если папка не пустая, используют rm -r "имя папки")
! rm удаляет файлы мгновенно и безвозвратно, нет корзины

Block 3 - Permissions

Вывод команды ls -l: -rw-rw-r-- 1 user user 6111 Aug 12 17:57 first-note.txt
1 символ "-" - тип файла, обычно это текст\картинка\скрипт; Также может быть "d" - директория; "l" символическая ссылка (Как ярлык в Windows)
rw-rw-r-- это права доступа - rw- = можно читать и изменять файл (User), второе rw- показывает для Group, r-- для остальных, то есть только чтение. 
далее идет "1" - это количество жестких ссылок (Это когда к одному файлу может вести несколько путей)
user user - первый user - это логин, второй user это группа владельца
далее идет размер файла, дата и имя

Скрипты - расширение .sh
Мы можем менять группы доступа на файлы. chmod +x myscript.sh, после выполнения этой команды, вывод ls -l myscript.sh будет: -rwxrwxr-x ... rwx - означает, что файл можно читать, изменять и запускать, r-x - означает, что можно запускать и читать.
chmod - сhange mode, chown - change owner
chmod 644 "имя файла" - изменение групп доступа: 6(Владелец) - 4(read) + 2(edit) = rw-; 4(Группа) - 4(read) - r--; 4(Остальные) - также как и группа. ! (ЗАПУСКАТЬ НЕТ)
chmod 755 "имя скрипта" для скриптов ( ЗАПУСКАТЬ ДА. Владелец также может изменять)
При отсутствии у скриптов rwx - скрипт будет запрещено запускать - ./hello.sh вывод - Permission Denied, после изменения: chmod 755 hello.sh, вывод ls -l: rwxr-xr-x, что означает, что только владелец может изменять данный скрипт, а остальные только запускать и читать
Если мы хотим запретить остальным запускать наш скрипт, то мы пишем chmod 700 "имя файла/скрипта"
r-- (4) — только чтение (конфиги, логи)
rw- (6) — чтение + запись (личные файлы)
r-x (5) — чтение + запуск (скрипты для всех)
rwx (7) — всё (твои скрипты)
--- (0) — ничего (запрет доступа)

Block 4 - searching

Иногда команды ls недостаточно.
grep "Желаемый текст" "имя файла, в котором будет идти поиск" - поиск слова в файле. Выводится содержимое, но нужное слово выделяется красным
find - поиск файлов; find . - точка в данном случае означает поиск с текущей папки; find -name "*.txt" - поиск всех файлов с форматом .txt; -iname - игнор регистра; find . -type d - поиск только директорий (можно использовать f и т.д.) 
find . -size +100M - поиск файлов более 100 мб; find . -mtime -1 - поиск файлов, которые изменялись в течении 1 дня; можно комбинировать с другими командами find . -name "*.txt" -delete
locate - быстрый поиск по базе данных, которая обновляется раз в определенный промежуток времени (быстрый поиск, но не видит свежие файлы)


Block 5 - Diagnostic

df - показывает сколько осталось место на диске; -h дает возможность посмотреть в мб и гб, а не в битах
free -h - показывает свободное ОЗУ
htop - что-то вроде диспетчера задач в винде.

Block 6 - users and permissions

id - Вывод покажет UID (номер пользователя), GID (номер группы) и все группы, в которых я состою
sudo useradd "login" - добавление нового пользователя; sudo userdel "login" - удаляет пользователя; если sudo userdel -r "login" - удаляет его домашний каталог вместе с пользователем; также для создания домашней директории для нового пользователя необходим флаг -m: useradd -m "login"
sudo passwd "login" - присвоение пользователю пароля
sudo usermod -aG "группа" "login" - добавление пользователя в группу, -aG - означает добавление; sudo grasswd -d "login" "группа" - удаляет пользователя из группы
su "login" - переключение на другого пользователя
exit - вернуться к предыдущему пользователю
chown - поменять владельца файла
sudo chgrp sudo "namefile" - изменение группы для файла, то есть в данном случае все, кто состоит в группе sudo могут открывать данный файл
! Если создать папку на другом пользователе и присвоить ее другому пользователю, необходимо предоставить ему доступ к пути, по которому возможно попасть к этому файлу или добавить пользователя в группу

Block 7 - operators

> - запись или перезаписывание, пример: echo "Hello" > hello.txt - если такого файла не существует, он создаст, если такой файл есть, то он перезапишет все, что там было на "Hello"
>> - добавление, пример: echo "World" >> hello.txt - добавляет информацию к имеющейся, вывод cat hello.txt - Hello/nWorld
| - труба, позволяет объединить команды - ls -l | grep ".txt" - выведет список только текстовых файлов и выделит .txt
wc -l - счетчик нужного - grep "error" log.txt | wc -l - вывод 3

Block 8 - Archive

tar -czvf "archive_name" "file_name ..." - Создание архива с флагами: c - создание архива; z - сжать его с помощью gzip, v - показать какие файлы добавляются; f - указать имя файла архива (всегда должен быть последним флагом)
tar -xzvf "archive_name" - разархивирование архива с теми же флагами, но x - извлечь
dd - Это низкоуровневая утилита для копирования и преобразования данных. Её называют "disk dump" или "data definition". Она работает на уровне блоков (сырых байтов), а не файлов.
пример: dd if=/dev/urandom of=large_file.bin bs=1M count=5
if - откуда читать данные; /dev/urandom - это специальный файл в Linux, который генерирует бесконечный поток псевдослучайных байтов; of="file_name" - выходной файл; bs (BlockSize) - сколько байт читать\записать за один раз; count=5 - сколько блоков прочитать и записать

Примеры bs=:
bs=512 — 512 байт (стандартный размер сектора диска)
bs=4K — 4 килобайта (размер страницы памяти)
bs=10M — 10 мегабайт (для больших файлов)

Другие варианты if=:
if=/dev/zero — генерирует бесконечный поток нулей (полезно для создания пустых файлов)
if=/dev/sda — читать с диска sda (опасно!)
if=backup.img — читать из обычного файла

Block 9 - Proccesses

ps aux - ps делает снимок процессов в данный момент, флаги a - всех пользователей показать; u - указывать пользователя; x - показать все процессы, даже фоновые
kill "PID" - мягко убивает процесс, позволяет ему сохраниться и закрыться
kill -9 "PID" - принудительно убивает процесс, если он не слушается
& - если добавить в конец команды, то он отправит ее в фоновый режим
jobs - посмотреть фоновые задачи; jobs -l - показывает подробную информацию о фоновом процессе 
fg - вернуть задачи на передний план
cron - планировщик задач, скрипты, которые срабатывают в указанный промежуток времени.
Пример: crontab -l - посмотреть текущие задачи; crontab -e - изменить текущие задчи\добавить;
* * * * * команда_для_выполнения
Каждая звездочка отвечает за свое время:
Минута (0-59)
Час (0-23)
День месяца (1-31)
Месяц (1-12)
День недели (0-7, где 0 и 7 — это воскресенье)
Пример:
0 3 * * * /home/user/backup.sh → Запускать скрипт бэкапа каждый день в 3:00 ночи.
0 9 * * 1 → Запускать каждый понедельник в 9:00 утра.
*/5 * * * * → Запускать каждые 5 минут.



